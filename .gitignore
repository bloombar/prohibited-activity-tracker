# clasp authentication (stored globally at ~/.clasprc.json, not here)
.clasprc.json

# macOS
.DS_Store

# Temporary directories created by deploy.sh
/tmp/

# Per-machine deployment records (script IDs vary by Google account)
/deployments/
